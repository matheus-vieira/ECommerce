@model IEnumerable<Front.Models.Cidade>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Estado.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CodigoIbge)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Estado.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CodigoIbge)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CidadeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CidadeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CidadeId })
        </td>
    </tr>
}

</table>
